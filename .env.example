# Скопируй в .env и заполни (файл .env в git не попадает).
# PAT: GitHub → Settings → Developer settings → Personal access tokens (classic)
# Права: repo + workflow
#
# PORTAL_GITHUB_TOKEN=ghp_xxxxxxxx
